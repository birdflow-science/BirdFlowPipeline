% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transition_loader.R
\name{load.TransitionsLoader}
\alias{load.TransitionsLoader}
\title{Load transitions into a TransitionsLoader (S3 method)}
\usage{
\method{load}{TransitionsLoader}(object, loading_function = get_transitions, ...)
}
\arguments{
\item{object}{A \code{\link[=TransitionsLoader]{TransitionsLoader()}}.}

\item{loading_function}{A function with signature \verb{function(loader)} that
returns the list described in \code{\link[=get_transitions]{get_transitions()}}.}
}
\value{
The modified \code{loader} (invisible) with \code{loader$transitions} populated.
}
\description{
Loads/combines banding, Motus, and tracking data via a \code{loading_function}
(default \code{\link[=get_transitions]{get_transitions()}}) and attaches the result to \code{loader$transitions}.
}
\examples{
\dontrun{
tl <- transitions_loader(trainer)
tl <- load(tl)  # populates tl$transitions
}
}
\seealso{
\code{\link[=get_transitions]{get_transitions()}}, \code{\link[=split.TransitionsLoader]{split.TransitionsLoader()}}
}
