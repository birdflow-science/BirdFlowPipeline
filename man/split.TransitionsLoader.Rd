% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transition_loader.R
\name{split.TransitionsLoader}
\alias{split.TransitionsLoader}
\title{Split transitions into training and test sets (S3 method)}
\usage{
\method{split}{TransitionsLoader}(object, splitting_function = train_test_split, seed = NULL, ...)
}
\arguments{
\item{object}{A \code{\link[=TransitionsLoader]{TransitionsLoader()}} that has been loaded (see \code{\link[=load.TransitionsLoader]{load.TransitionsLoader()}}).}

\item{splitting_function}{Function with signature \verb{function(loader, seed, ...)}
that returns the list described in \code{\link[=train_test_split]{train_test_split()}}.}

\item{seed}{Optional integer to make the split reproducible.}

\item{...}{Passed through to \code{splitting_function}.}
}
\value{
A list with two elements:
\describe{
\item{training_data}{List with \code{train_data} and \code{train_data_one_week} (both \code{\link[BirdFlowR:Routes-internal]{BirdFlowR::BirdFlowIntervals()}}).}
\item{test_data}{List with \code{test_data} and \code{test_data_one_week} (both \code{\link[BirdFlowR:Routes-internal]{BirdFlowR::BirdFlowIntervals()}}).}
}
}
\description{
Uses a \code{splitting_function} (default \code{\link[=train_test_split]{train_test_split()}}) to create
training/test splits for interval data and 1-week interval data. Returns a
list containing both training and test bundles.
}
\examples{
\dontrun{
tl <- load(transitions_loader(trainer))
parts <- split(tl, seed = 42)
names(parts)
}
}
\seealso{
\code{\link[=train_test_split]{train_test_split()}}, \code{\link[=load.TransitionsLoader]{load.TransitionsLoader()}}
}
