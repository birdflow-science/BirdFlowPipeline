% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transition_loader.R
\name{train_test_split}
\alias{train_test_split}
\title{Train/test split helper for transitions}
\usage{
train_test_split(loader, seed = NULL, training_n_transitions = NULL)
}
\arguments{
\item{loader}{A loaded \code{\link[=TransitionsLoader]{TransitionsLoader()}} (contains \verb{$transitions}).}

\item{seed}{Optional integer for reproducibility (\code{set.seed()}).}

\item{training_n_transitions}{Optional integer. If supplied, subsamples the
training intervals to exactly this number (errors if insufficient data).}
}
\value{
A list:
\describe{
\item{training_data}{List with \code{train_data}, \code{train_data_one_week}.}
\item{test_data}{List with \code{test_data}, \code{test_data_one_week}.}
}
}
\description{
Creates a random 70/30 split of interval transitions and one-week transitions,
with optional downsampling of the training set to a fixed number of transitions
(useful for learning-curve experiments). Returns four
\code{\link[BirdFlowR:BirdFlowIntervals]{BirdFlowR::BirdFlowIntervals()}} objects grouped into training/test lists.
}
\details{
Side effects: none (beyond RNG if \code{seed} is provided).
}
\examples{
\dontrun{
tl <- load(transitions_loader(trainer))
parts <- train_test_split(tl, seed = 1, training_n_transitions = 1000)
}
}
\seealso{
\code{\link[=split.TransitionsLoader]{split.TransitionsLoader()}}, \code{\link[=get_transitions]{get_transitions()}}
}
