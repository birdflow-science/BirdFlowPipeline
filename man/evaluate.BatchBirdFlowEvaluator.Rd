% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch_evaluator.R
\name{evaluate.BatchBirdFlowEvaluator}
\alias{evaluate.BatchBirdFlowEvaluator}
\title{Evaluate a set of BirdFlow HDF5 models (S3 method)}
\usage{
\method{evaluate}{BatchBirdFlowEvaluator}(evaluator, data, evaluation_function = evaluate_model)
}
\arguments{
\item{evaluator}{A \code{\link[=BatchBirdFlowEvaluator]{BatchBirdFlowEvaluator()}}.}

\item{data}{A list or object containing evaluation data. Typically
\code{split_data$training_data} or \code{split_data$test_data}, where index \verb{[[1]]}
is interval data and \verb{[[2]]} is 1-week data used for PIT calibration.}

\item{evaluation_function}{Function of signature
\verb{function(bf_path, data, params)} returning a result per model (defaults to
\code{\link[=evaluate_model]{evaluate_model()}}).}
}
\value{
A list as returned by \code{\link[batchtools:reduceResultsList]{batchtools::reduceResultsList()}} (often a list
of per-model lists/data frames produced by \code{evaluation_function}).
Side effects: writes PIT objects to disk (if done by \code{evaluation_function}),
prints job status.
}
\description{
Submits evaluation jobs (one per HDF5) via \strong{batchtools} using the provided
\code{evaluation_function} (defaults to \code{\link[=evaluate_model]{evaluate_model()}}). Retries failed/expired
jobs up to two times. Returns a list of results gathered with
\code{\link[batchtools:reduceResultsList]{batchtools::reduceResultsList()}}.
}
\details{
HDF5 models are discovered in \code{evaluator$batch_trainer$params$hdf_dir}
matching the current species and resolution. A \strong{batchtools} registry is
created under \code{output_path} with a timestamped suffix.
}
\examples{
\dontrun{
ev <- BatchBirdFlowEvaluator(trainer)
res <- evaluate(ev, split_data$test_data)
}
}
\seealso{
\code{\link[=evaluate_model]{evaluate_model()}}, \code{\link[=BatchBirdFlowEvaluator]{BatchBirdFlowEvaluator()}}, \code{\link[batchtools:reduceResultsList]{batchtools::reduceResultsList()}}
}
