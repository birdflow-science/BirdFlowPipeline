% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch_evaluator.R
\name{evaluate_model}
\alias{evaluate_model}
\title{Evaluate a single BirdFlow model (default evaluation function)}
\usage{
evaluate_model(bf_path, data, params)
}
\arguments{
\item{bf_path}{Path to a fitted BirdFlow HDF5 file.}

\item{data}{A list where \verb{[[1]]} holds interval data and \verb{[[2]]} holds
one-week interval data used for PIT calibration.}

\item{params}{Parameter list (usually \code{evaluator$batch_trainer$params})
providing \code{output_path}, \code{season}, and other configuration used by helpers.}
}
\value{
A list with elements:
\describe{
\item{df}{A one-row tibble of summary metrics and hyperparameters.}
\item{obs}{The interval data object passed in \code{data[[1]]}.}
\item{metric_for_each_transition}{Per-transition metrics table.}
}
}
\description{
Loads a fitted BirdFlow HDF5, computes interval-based metrics, optional
route statistics (both conditional on observed tracks and unconditional
synthetic routes by season), and PIT calibration diagnostics. Also extracts
key hyperparameters and distribution-level correlations.
}
\details{
Side effects:
\itemize{
\item Saves a PIT calibration RDS to \verb{<output_path>/pit_data/}.
\item Prints a compact tibble of derived metrics.
}
}
\examples{
\dontrun{
one <- split_data$training_data
two <- split_data$training_data_one_week
out <- evaluate_model(".../amewoo_150km_xyz.hdf5", data = list(one, two), params = trainer$params)
out$df
}
}
\seealso{
\code{\link[=evaluate.BatchBirdFlowEvaluator]{evaluate.BatchBirdFlowEvaluator()}}, \code{\link[BirdFlowR:calc_interval_metrics]{BirdFlowR::calc_interval_metrics()}},
\code{\link[BirdFlowR:distribution_performance]{BirdFlowR::distribution_performance()}}, \code{\link[BirdFlowR:route]{BirdFlowR::route()}}
}
