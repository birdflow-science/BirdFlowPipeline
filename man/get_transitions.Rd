% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transition_loader.R
\name{get_transitions}
\alias{get_transitions}
\title{Get ground-truth transitions for tuning and validation}
\usage{
get_transitions(loader, max_n_intervals = 10000)
}
\arguments{
\item{loader}{A \code{\link[=TransitionsLoader]{TransitionsLoader()}} object.}

\item{max_n_intervals}{the maximum intervals to sample.}
}
\value{
A list with:
\describe{
\item{combined_routes_data}{\code{data.frame} of stacked banding, Motus, and tracking rows (NA-dropped).}
\item{interval_obj}{\code{\link[BirdFlowR:BirdFlowIntervals]{BirdFlowR::BirdFlowIntervals()}} with 1–180 day / 0–8000 km constraints, filtered to migration seasons.}
\item{interval_one_week_obj}{\code{\link[BirdFlowR:BirdFlowIntervals]{BirdFlowR::BirdFlowIntervals()}} with exactly 1-day steps (weekly objects), filtered to migration seasons.}
}
}
\description{
Functions to get ground truth routes and transitions for model tuning and
validation by combining tracking, Motus, and banding sources, then converting
to \code{\link[BirdFlowR:BirdFlowIntervals]{BirdFlowR::BirdFlowIntervals()}} objects (full intervals and 1-week intervals).
}
\details{
Side effects:
\itemize{
\item Resolves/prints a \code{source} string reflecting which datasets were found.
\item Errors if no transitions are available or if fewer than \eqn{10/0.7} intervals remain for tuning.
}
}
\examples{
\dontrun{
tl <- TransitionsLoader(trainer)
gt <- get_transitions(tl)
str(gt$interval_obj$data)
}
}
\seealso{
\code{\link[=TransitionsLoader]{TransitionsLoader()}}, \code{\link[=split.TransitionsLoader]{split.TransitionsLoader()}}, \code{\link[=train_test_split]{train_test_split()}},
\code{\link[BirdFlowR:Routes]{BirdFlowR::Routes()}}, \code{\link[BirdFlowR:as_BirdFlowRoutes]{BirdFlowR::as_BirdFlowRoutes()}}, \code{\link[BirdFlowR:as_BirdFlowIntervals]{BirdFlowR::as_BirdFlowIntervals()}}
}
